version: '3.8'

services:
  user-api:
    build: .
    container_name: user-api
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - SPRING_PROFILES_ACTIVE=default
    restart: unless-stopped
